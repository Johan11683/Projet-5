import React from 'react';
import ReactDOM from 'react-dom/client';
import AppRouter from './components/Router';  // On importe notre composant Router

ReactDOM.createRoot(document.getElementById('root')).render(<AppRouter />);
